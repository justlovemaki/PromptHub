# 国际化扩展

本文档介绍如何扩展 PromptHub 的多语言支持。

## 🌍 当前支持的语言

- 简体中文 (`zh-CN`)
- 英文 (`en`)
- 日文 (`ja`)

## ➕ 添加新语言

### 1. 更新语言常量

编辑 [`src/lib/constants.ts`](../../src/lib/constants.ts)：

```typescript
export const LANGUAGES = {
  ENGLISH: 'en',
  CHINESE_SIMPLIFIED: 'zh-CN',
  JAPANESE: 'ja',
  KOREAN: 'ko',           // 新增韩语
  FRENCH: 'fr',           // 新增法语
} as const;

export const LANGUAGE_NAMES: Record<string, string> = {
  'en': 'English',
  'zh-CN': '简体中文',
  'ja': '日本語',
  'ko': '한국어',         // 新增
  'fr': 'Français',       // 新增
};
```

### 2. 更新 i18n 配置

编辑 [`src/i18n/settings.ts`](../../src/i18n/settings.ts)：

```typescript
export const languages = ['en', 'zh-CN', 'ja', 'ko', 'fr'];
export const defaultLanguage = 'en';

export const languageNames: Record<string, string> = {
  'en': 'English',
  'zh-CN': '简体中文',
  'ja': '日本語',
  'ko': '한국어',
  'fr': 'Français',
};
```

### 3. 创建翻译文件

在 `public/locales/` 下创建新语言目录：

```bash
mkdir public/locales/ko
mkdir public/locales/fr
```

复制现有翻译文件作为模板：

```bash
cp public/locales/en/*.json public/locales/ko/
cp public/locales/en/*.json public/locales/fr/
```

### 4. 翻译文件内容

翻译每个 JSON 文件中的文案。

**示例：`public/locales/ko/common.json`**

```json
{
  "appName": "PromptHub",
  "loading": "로딩 중...",
  "error": "오류가 발생했습니다",
  "success": "성공",
  "cancel": "취소",
  "confirm": "확인",
  "save": "저장",
  "delete": "삭제",
  "edit": "편집",
  "search": "검색",
  "noData": "데이터가 없습니다"
}
```

### 5. 更新 SEO 配置

SEO 配置通过翻译文件管理。在新语言的 `layout.json` 文件中添加 SEO 相关字段：

**示例：`public/locales/ko/layout.json`**

```json
{
  "title": "PromptHub - AI 프롬프트 관리 플랫폼",
  "description": "전문적인 AI 프롬프트 관리 도구로 풍부한 템플릿 라이브러리를 제공합니다.",
  "seo": {
    "siteName": "PromptHub",
    "siteDescription": "전문적인 AI 프롬프트 관리 도구로 풍부한 템플릿 라이브러리를 제공합니다.",
    "siteKeywords": "AI 프롬프트, ChatGPT 프롬프트, Claude 프롬프트, 프롬프트 관리",
    "siteUrl": "",
    "ogImage": "/logo.png",
    "twitterHandle": "@prompthub",
    "twitterCard": "summary_large_image"
  }
}
```

**示例：`public/locales/fr/layout.json`**

```json
{
  "title": "PromptHub - Plateforme de gestion de prompts IA",
  "description": "Outil professionnel de gestion de prompts IA avec une riche bibliothèque de modèles.",
  "seo": {
    "siteName": "PromptHub",
    "siteDescription": "Outil professionnel de gestion de prompts IA avec une riche bibliothèque de modèles.",
    "siteKeywords": "prompt IA, prompts ChatGPT, prompts Claude, gestionnaire de prompts",
    "siteUrl": "",
    "ogImage": "/logo.png",
    "twitterHandle": "@prompthub",
    "twitterCard": "summary_large_image"
  }
}
```

> **注意**: `siteUrl` 字段留空时，系统会自动使用环境变量 `BETTER_AUTH_URL` 或 `NEXT_PUBLIC_BASE_URL` 的值。

### 6. 更新 Sitemap 生成

编辑 [`scripts/generate-sitemap.js`](../../scripts/generate-sitemap.js)：

```javascript
const languages = ['en', 'zh-CN', 'ja', 'ko', 'fr'];

// 生成每种语言的 sitemap
languages.forEach(lang => {
  generateSitemap(lang);
});
```

### 7. 更新 next-sitemap 配置

编辑 [`next-sitemap.config.js`](../../next-sitemap.config.js)：

```javascript
module.exports = {
  // ...
  alternateRefs: [
    { href: 'https://your-domain.com/en', hreflang: 'en' },
    { href: 'https://your-domain.com/zh-CN', hreflang: 'zh-CN' },
    { href: 'https://your-domain.com/ja', hreflang: 'ja' },
    { href: 'https://your-domain.com/ko', hreflang: 'ko' },
    { href: 'https://your-domain.com/fr', hreflang: 'fr' },
  ],
};
```

## 📁 翻译文件结构

```
public/locales/
├── en/
│   ├── common.json      # 通用文案
│   ├── home.json        # 首页
│   ├── landing.json     # 落地页
│   ├── dashboard.json   # 仪表板
│   ├── explore.json     # 广场
│   ├── prompt.json      # 提示词
│   ├── admin.json       # 管理后台
│   ├── account.json     # 账户
│   ├── layout.json      # 布局
│   ├── user.json        # 用户
│   ├── logs.json        # 日志
│   ├── contact.json     # 联系
│   ├── privacy.json     # 隐私政策
│   ├── terms.json       # 服务条款
│   └── download.json    # 下载
├── zh-CN/
├── ja/
├── ko/                  # 新增
└── fr/                  # 新增
```

## 🔧 使用翻译

### 服务端组件

```typescript
import { getTranslation } from '@/i18n';

export default async function Page({
  params: { lang }
}: {
  params: { lang: string }
}) {
  const { t } = await getTranslation(lang, 'common');
  
  return <h1>{t('appName')}</h1>;
}
```

### 客户端组件

```typescript
'use client';

import { useTranslation } from '@/i18n/client';

export function ClientComponent({ lang }: { lang: string }) {
  const { t, i18n } = useTranslation(lang, 'common');
  
  return <button>{t('save')}</button>;
}
```

### 多命名空间

```typescript
const { t } = await useTranslation(lang, ['common', 'dashboard']);

// 使用
t('common:save')
t('dashboard:title')
```

### 带参数的翻译

**翻译文件：**

```json
{
  "welcome": "欢迎，{{name}}！",
  "itemCount": "共 {{count}} 项"
}
```

**使用：**

```typescript
t('welcome', { name: 'John' })  // "欢迎，John！"
t('itemCount', { count: 10 })   // "共 10 项"
```

## 🔄 语言切换

### 语言切换组件

项目已包含语言切换组件 [`src/components/LanguageSwitcher.tsx`](../../src/components/LanguageSwitcher.tsx)。

### 自定义语言切换

```typescript
'use client';

import { useRouter, usePathname } from 'next/navigation';
import { languages, languageNames } from '@/i18n/settings';

export function LanguageSwitcher({ currentLang }: { currentLang: string }) {
  const router = useRouter();
  const pathname = usePathname();

  const switchLanguage = (newLang: string) => {
    // 替换路径中的语言代码
    const newPath = pathname.replace(`/${currentLang}`, `/${newLang}`);
    router.push(newPath);
  };

  return (
    <select
      value={currentLang}
      onChange={(e) => switchLanguage(e.target.value)}
      className="border rounded px-2 py-1"
    >
      {languages.map((lang) => (
        <option key={lang} value={lang}>
          {languageNames[lang]}
        </option>
      ))}
    </select>
  );
}
```

## 📝 翻译最佳实践

### 1. 使用有意义的 Key

```json
// ✅ 好
{
  "button.save": "保存",
  "button.cancel": "取消",
  "error.networkError": "网络错误"
}

// ❌ 不好
{
  "btn1": "保存",
  "btn2": "取消",
  "err1": "网络错误"
}
```

### 2. 避免拼接字符串

```typescript
// ✅ 好
t('welcome', { name: userName })

// ❌ 不好
t('hello') + ' ' + userName
```

### 3. 保持翻译文件同步

确保所有语言的翻译文件包含相同的 key。

### 4. 使用翻译管理工具

推荐工具：
- [i18n Ally](https://marketplace.visualstudio.com/items?itemName=Lokalise.i18n-ally) - VS Code 扩展
- [Crowdin](https://crowdin.com/) - 翻译管理平台
- [Lokalise](https://lokalise.com/) - 翻译管理平台

## ✅ 国际化检查清单

### 添加新语言

- [ ] 更新 `constants.ts` 中的语言常量
- [ ] 更新 `i18n/settings.ts` 配置
- [ ] 创建翻译文件目录
- [ ] 翻译所有 JSON 文件
- [ ] 更新 SEO 配置
- [ ] 更新 Sitemap 生成脚本
- [ ] 测试语言切换功能

### 添加新文案

- [ ] 在所有语言的翻译文件中添加 key
- [ ] 使用有意义的 key 名称
- [ ] 测试翻译显示

## 下一步

- 了解 [移除提示词功能](./removing-prompts.md)
- 阅读 [品牌定制](./branding.md)
- 查看 [功能扩展](./extending.md)